<snippet>
    <content><![CDATA[
for (${1:var }${2:k} in ${3:obj}) {
    if (!${3}.hasOwnProperty(${2})) {${4: }continue;${5: }}${0}
}
]]></content>
    <!-- Optional: Tab trigger to activate the snippet -->
    <tabTrigger>///forin</tabTrigger>
    <!-- Optional: Scope the tab trigger will be active in -->
    <scope>source.js, source.embedded.js.html</scope>
    <!-- Optional: Description to show in the menu -->
    <description>JS for...in loop boilerplate</description>
</snippet>
